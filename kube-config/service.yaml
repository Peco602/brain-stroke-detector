apiVersion: v1
kind: Service
metadata:
  name: brain-stroke-detector
spec:
  type: LoadBalancer
  selector:
    app: brain-stroke-detector
  ports:
  - name: flask
    port: 8080
    targetPort: 8080
  - name: streamlit
    port: 8501
    targetPort: 8501
